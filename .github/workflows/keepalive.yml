name: keepalive
on:
  schedule:
    - cron: '*/7 * * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Wake chat server
        run: curl -fsS --max-time 60 https://daytrack-chat.onrender.com/api/chat/nicks